import { fetchPurchases, postPurchases, deletePurchases } from './apiCalls';

describe('Api Calls', () => {
  const mockResponse = {
    id: 1,
    name: 'harry',
    description:' hairy fella',
    price: 1,
    imgUrl: 'something'
  }
});